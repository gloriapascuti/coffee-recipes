import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
});

//
// import { render, screen } from '@testing-library/react';
// import App from './App';
//
// test('renders App component', () => {
//   // Render the App component
//   render(<App />);
//
//   // Check if Header is rendered (assuming your Header component is present)
//   const headerElement = screen.getByText(/generalities/i); // Adjust text if needed to match actual content
//
//   // Ensure the element exists in the document
//   expect(headerElement).toBeInTheDocument();
// });


